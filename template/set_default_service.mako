% if default_service:
Current default service is <b>${default_service}</b>, which service do you want to set as new default?
% else:
You have not select default service, which service do you want to set as default?
% endif